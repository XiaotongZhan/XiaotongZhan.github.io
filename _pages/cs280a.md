---
title: "CS 280A: Intro to Computer Vision and Computational Photography"
layout: assignments_archive
permalink: /cs280a/
comments: false
---

<style>
  .assignment-grid {
    display: grid !important;
    grid-template-columns: 1fr !important; /* 单列 */
    gap: 1rem;
  }
  .assignment-card {
    width: 100%;
    max-width: 780px;     /* 可选：让卡片不至于太宽 */
    margin: 0 auto;       /* 居中 */
  }
</style>

<div class="assignment-grid">
  {% assign items = site.assignments | where: "course", "CS280A" | sort: "date" | reverse %}
  {% for post in items %}
    <div class="assignment-card" onclick="location.href='{{ post.url | relative_url }}'">
      <h2>{{ post.title }}</h2>

      <p class="assignment-date">
        {{ post.date | date: "%Y-%m-%d" }}
        · <i class="fa fa-clock" aria-hidden="true"></i> {% include read-time.html page=post %}
      </p>
    </div>
  {% endfor %}
</div>

